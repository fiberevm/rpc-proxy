# README banner

`rpc-proxy-banner.png` was generated with the built-in image-generation tool. The product name is **RPC Proxy**; **Fiber** is the creator credit, not part of the product name.

## Generation prompt

```text
Use case: ads-marketing
Asset type: a polished, very wide GitHub README banner, approximately 3:1 landscape.
Primary request: create a distinctive banner for an open-source infrastructure project named "RPC Proxy", with only a small creator credit "by Fiber".
Scene/backdrop: deep graphite background with restrained, subtle texture.
Subject: a precise abstract fiber-like routing motif: several fine independent streams converge into one clean coherent path, visually suggesting guaranteed consistency of RPC reads across providers. Keep this understated and architectural, not a literal labeled diagram.
Style/medium: premium editorial technology artwork, crisp typography and finely rendered dimensional linework, generous negative space, restrained monochrome silver and ivory on graphite. This is a finished banner image, not a website mockup.
Composition/framing: wide panoramic banner; strong typographic hierarchy and ample safe margins. The product name is the unmistakable focus, large and readable even when displayed at 800px wide. Keep the delicate routing artwork separate from the text so it never reduces legibility.
Text (verbatim): "RPC Proxy" as the large headline; "by Fiber" as the much smaller credit.
Constraints: those are the only words. The product is named RPC Proxy, never Fiber RPC Proxy. Do not invent a Fiber logo. No badges, no extra slogans, no fake code, no cryptocurrency symbols, no UI panels, no watermark. Avoid a busy network diagram, excessive glow, generic neon gradients, or cramped typography. Render both text strings accurately exactly once.
```
